package:
  name: ros-noetic-carrot-planner
  version: 1.17.1
source:
  git_url: https://github.com/ros-gbp/navigation-release.git
  git_rev: release/noetic/carrot_planner/1.17.1-1
  folder: ros-noetic-carrot-planner/src/work

build:
  script:
    sel(win): bld_catkin.bat
    sel(unix): build_catkin.sh
  number: 4
about:
  home: https://www.ros.org/
  license: BSD-3-Clause
  summary: |
    Robot Operating System

extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - "{{ compiler('cxx') }}"
    - "{{ compiler('c') }}"
    - ninja
    - sel(unix): make
    - cmake
  host:
    - eigen
    - python
    - ros-distro-mutex 0.1 noetic
    - ros-noetic-base-local-planner
    - ros-noetic-catkin
    - ros-noetic-costmap-2d
    - ros-noetic-nav-core
    - ros-noetic-pluginlib
    - ros-noetic-roscpp
    - ros-noetic-tf2
    - ros-noetic-tf2-geometry-msgs
    - ros-noetic-tf2-ros
  run:
    - eigen
    - python
    - ros-distro-mutex 0.1 noetic
    - ros-noetic-base-local-planner
    - ros-noetic-costmap-2d
    - ros-noetic-nav-core
    - ros-noetic-pluginlib
    - ros-noetic-roscpp
    - ros-noetic-tf2
    - ros-noetic-tf2-ros
